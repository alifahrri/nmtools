codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: yes
      macro: yes

ignore:
  - "/usr/include/**/*"        # ignore system lib
  - "/usr/local/include/**/*"  # ignore 3rd parties